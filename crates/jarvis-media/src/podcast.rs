/// Podcast production engine.
pub struct PodcastProducer;

impl PodcastProducer {
    pub fn new() -> Self {
        Self
    }
}

impl Default for PodcastProducer {
    fn default() -> Self {
        Self::new()
    }
}
