/// Image generation engine (ultra-realistic).
pub struct ImageGenerator;

impl ImageGenerator {
    pub fn new() -> Self {
        Self
    }
}

impl Default for ImageGenerator {
    fn default() -> Self {
        Self::new()
    }
}
