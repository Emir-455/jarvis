/// Image processing and generation module.
pub struct JarvisImage;

impl JarvisImage {
    pub fn new() -> Self {
        Self
    }
}

impl Default for JarvisImage {
    fn default() -> Self {
        Self::new()
    }
}
